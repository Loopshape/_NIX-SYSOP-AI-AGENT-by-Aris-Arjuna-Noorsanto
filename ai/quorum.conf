# Minimum agents required for an action
QUORUM_MIN=3

# Mandatory validators (must be present)
QUORUM_CORE=(CUBE CORE LOOP)

# Tool execution requires:
TOOL_QUORUM=(CORE CODE WORK)

# External IO (REST / URL / SOAP)
IO_QUORUM=(CORE LINE WAVE)

# Self-modifying / generator actions
GEN_QUORUM=(CORE COIN CODE)

# Emergency fallback (low-memory / degraded)
FALLBACK_QUORUM=(CORE LOOP)

