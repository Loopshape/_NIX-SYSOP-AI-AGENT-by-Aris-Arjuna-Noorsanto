#!/usr/bin/env bash
# Tool for interacting with the CODE agent

PROMPT=""
if [ -z "" ]; then
    echo "Usage: $0 <prompt>"
    exit 1
fi

MODEL="phi:2.7b"

curl -s -X POST http://127.0.0.1:11434/api/generate -d "{
    \"model\": \"\",
    \"prompt\": \"\" 
}"
